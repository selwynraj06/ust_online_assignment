package com.simplemovie.controller;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RestController;

import com.simplemovie.entity.Shows;
import com.simplemovie.service.ShowsService;

@RestController
public class ShowsController {
	
	@Autowired
	ShowsService showServ;
	
	// http://localhost:8080/api/addshows
	@PostMapping("/api/addshows")
	public Shows addShow(@RequestBody Shows show) {
		return showServ.addShows(show);
	}
	
	public List<Shows> showAllDetails() {
		return showServ.showAllShows();
	}
	
}
