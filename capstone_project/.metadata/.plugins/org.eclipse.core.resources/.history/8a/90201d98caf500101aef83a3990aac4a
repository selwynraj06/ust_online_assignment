package com.simplemovie.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.simplemovie.entity.Shows;

public interface BookingRepository extends JpaRepository<Shows, Integer>{
	
	
}
